#!/bin/sh
 
# set $SSH_ORIGINAL_COMMAND
case "$2" in
   "nc -q0 dev.local 22")
     nc -q0 dev.local 22
     exit 0
     ;;
   *)
     logger -s -t restricted-command -- "Invalid command $2"
     exit 1
     ;;
esac
 
# logger -t restricted-command -- "Executing $@"
